{
  "format": "studio-mcp-v2-upstream-fixture",
  "catalog_version": "fixture-2",
  "tools": [
    {
      "name": "existing_unmapped_tool",
      "description": "An unchanged baseline-only operation.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    },
    {
      "name": "studio_structured_script_read",
      "description": "Fixture addition mapped by an operator-owned exact-handler policy.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "minItems": 1,
            "maxItems": 64
          },
          "max_chars": {
            "type": "integer",
            "minimum": 1,
            "maximum": 262144
          }
        },
        "required": [
          "path"
        ],
        "additionalProperties": false
      }
    }
  ]
}

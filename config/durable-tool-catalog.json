{
  "format": "studio-mcp-v2-durable-catalog",
  "catalog_version": "0.4.0-dev.1",
  "upstream": {
    "version": "production-v1-snapshot-2026-07-26",
    "source_sha256": "96130d76240be565f98417414f86e0776a5b73d880f4fadbc5de0bd1c915535b",
    "compatibility": "reviewed-local-subset-only"
  },
  "tools": [
    {
      "name": "studio_get_state",
      "description": "Read bounded connection, document, controller-predicate, and Play-transition state from one explicitly targeted Studio session. available_datamodel_types lists only general-operation request channels that are actually routable through v2; the lifecycle-only PlayServer bridge is never reported as a Server or Client route.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Get Studio State",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "studio_list_tree",
      "description": "Query a bounded, deterministic descendant tree beneath one exact Studio instance path. Optional filters use literal matching only. The response retains root_path, items, truncated, max_depth, and max_results for compatibility and adds returned, scanned, scan_limit, page_size, has_more, continuation_cursor, and the normalized filters. A returned continuation_cursor is opaque and bound to the explicit Studio session, document epoch, generation, query, ordering, and continuation lineage.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "root_path": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "minItems": 0,
            "maxItems": 64,
            "description": "Exact child-name segments from the DataModel root; an empty array means the DataModel."
          },
          "max_depth": {
            "type": "integer",
            "minimum": 0,
            "maximum": 6,
            "description": "Maximum descendant depth. The root_path segment count plus max_depth must not exceed the shared 64-segment reusable path bound."
          },
          "max_results": {
            "type": "integer",
            "minimum": 1,
            "maximum": 500,
            "description": "Legacy page-size alias retained for compatibility. Range 1 through 500. Do not combine with page_size."
          },
          "name_filter": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "pattern": "^[^\\u0000-\\u001f\\u007f]+$",
            "description": "Literal substring matched against Instance.Name, case-insensitive for ASCII letters and byte-literal for other UTF-8. Patterns and control characters are not accepted; the UTF-8 encoding is limited to 100 bytes."
          },
          "class_filter": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "pattern": "^[A-Za-z_][A-Za-z0-9_]*$",
            "description": "Case-sensitive literal Roblox class name identifier. Uses exact ClassName equality unless class_is_a is true; the UTF-8 encoding is limited to 100 bytes."
          },
          "class_is_a": {
            "type": "boolean",
            "description": "When true, evaluate class_filter with Instance:IsA instead of exact ClassName equality. Invalid unless class_filter is supplied."
          },
          "scan_limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 5000,
            "description": "Maximum number of deterministic traversal candidates examined in this call. Default: 2000."
          },
          "page_size": {
            "type": "integer",
            "minimum": 1,
            "maximum": 500,
            "description": "Maximum number of matching items returned in this page. Default: max_results when supplied, otherwise 200. Do not combine with max_results."
          },
          "continuation_cursor": {
            "type": "string",
            "minLength": 1,
            "maxLength": 512,
            "pattern": "^[A-Za-z0-9+/]+={0,2}\\.[0-9a-f]{64}$",
            "description": "Opaque cursor returned by a prior page for the same explicit Studio session, client, document epoch, generation, root, filters, depth, limits, ordering version, and unchanged continuation lineage. Never construct or reuse it for another query."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "annotations": {
        "title": "List Studio Tree",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "studio_read_script",
      "description": "Read bounded editor source and its SHA-256 revision from one exact LuaSourceContainer path.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "minItems": 1,
            "maxItems": 64
          },
          "max_chars": {
            "type": "integer",
            "minimum": 1,
            "maximum": 262144
          }
        },
        "required": [
          "path"
        ],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Read Studio Script",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "studio_update_script",
      "description": "Compare-and-swap one exact LuaSourceContainer using the current SHA-256 revision before applying bounded source.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "minItems": 1,
            "maxItems": 64
          },
          "expected_sha256": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$"
          },
          "new_source": {
            "type": "string",
            "maxLength": 262144
          }
        },
        "required": [
          "path",
          "expected_sha256",
          "new_source"
        ],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Update Studio Script",
        "readOnlyHint": false,
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "studio_set_attribute",
      "description": "Compare an exact prior attribute state, then create, replace, or remove one bounded primitive attribute.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "minItems": 1,
            "maxItems": 64
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "expected_exists": {
            "type": "boolean"
          },
          "expected_value_type": {
            "type": "string",
            "enum": [
              "none",
              "string",
              "number",
              "boolean"
            ]
          },
          "expected_value": {},
          "value_type": {
            "type": "string",
            "enum": [
              "remove",
              "string",
              "number",
              "boolean"
            ]
          },
          "value": {}
        },
        "required": [
          "path",
          "name",
          "expected_exists",
          "expected_value_type",
          "value_type"
        ],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Set Studio Attribute",
        "readOnlyHint": false,
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "studio_get_console",
      "description": "Read a bounded tail of Studio log history from one explicitly targeted Studio session.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "max_entries": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Get Studio Console",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "studio_capture_screenshot",
      "description": "Capture a permission-gated bounded Studio screenshot and return PNG bytes as base64.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "max_width": {
            "type": "integer",
            "minimum": 64,
            "maximum": 1280
          },
          "max_height": {
            "type": "integer",
            "minimum": 64,
            "maximum": 720
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Capture Studio Screenshot",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "studio_fire_input_binding",
      "description": "Fire one existing Scriptable InputBinding at an exact Studio path; this cannot inject keys, mouse input, OS input, or Studio UI input.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "minItems": 1,
            "maxItems": 64
          },
          "state_type": {
            "type": "string",
            "enum": [
              "boolean",
              "number",
              "vector2",
              "vector3"
            ]
          },
          "state": {}
        },
        "required": [
          "path",
          "state_type",
          "state"
        ],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Fire Scriptable Input Binding",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "studio_start_stop_play",
      "description": "Accept one guarded Start or Stop transition in the explicitly targeted Studio session. Start returns a correlated starting receipt and Stop returns a correlated stopping receipt; observe studio_get_state_v2 until the exact transition reaches Play or Edit.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "is_start": {
            "type": "boolean"
          }
        },
        "required": [
          "is_start"
        ],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Start or Stop Studio Play",
        "readOnlyHint": false,
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": false
      }
    }
  ]
}
